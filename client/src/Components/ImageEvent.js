import React from "react";
import "../styles/toolbar.css";
import menu from "../text.json";
const menuItems = menu.menuItems;

export default function ImageEvent() {
	return (
			<span id="image-window">
				hello
			</span>
		);
}